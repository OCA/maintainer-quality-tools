# List the OCA project dependencies, one per line
# Add a repository url and branch if you need a forked version

dependency-repo https://github.com/OCA/maintainer-quality-tools.git test-dependencies
